# CLAUDE.md

See [`AGENTS.md`](./AGENTS.md) for the project operating guide. Claude Code reads
skills from `.claude/skills/`. Run `npm test` and `npm run build` to verify changes.
Project MCP config: `.mcp.json` (Playwright).
